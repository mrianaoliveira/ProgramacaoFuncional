public class rotEsq {
   public void main(String[] args) 
   {
        int aux;
        if ( i >= f ){
            return v;
        }
        else{
            aux = v[i];
            v[i] = v[f];
            v[f] = aux;
            return inverte(v, i+1, f-1);
        }
   }
}
